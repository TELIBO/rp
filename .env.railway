# Railway Environment Variables
# Copy these to your Railway dashboard: Settings → Environment Variables

# Server Configuration
NODE_ENV=production
PORT=5000

# Document and Index Paths (use demo documents for deployment)
DOCUMENTS_PATH=./documents-demo
INDEX_PATH=./index

# PostgreSQL Database
# This will be auto-populated when you add PostgreSQL in Railway
# Railway → Add Service → Database → PostgreSQL
DATABASE_URL=${DATABASE_URL}

# Optional: Qdrant Vector Database
# Leave empty or remove if not using external Qdrant
# QDRANT_URL=

# IMPORTANT NOTES:
# 1. Do NOT commit this file to git (it's in .gitignore)
# 2. Add PostgreSQL in Railway dashboard first
# 3. DATABASE_URL will be automatically set by Railway
# 4. DOCUMENTS_PATH points to documents-demo (not your private documents/)
