# 🏗️ System Architecture

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                            │
│                     (React Frontend)                             │
│                   http://localhost:3000                          │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  SearchBar   │  │   Filters    │  │  Statistics  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                   │
│  ┌──────────────────────────────────────────────────┐          │
│  │           SearchResults Component                │          │
│  │  ┌────────┐ ┌────────┐ ┌────────┐              │          │
│  │  │ Card 1 │ │ Card 2 │ │ Card 3 │  ...         │          │
│  │  └────────┘ └────────┘ └────────┘              │          │
│  └──────────────────────────────────────────────────┘          │
│                                                                   │
│  ┌──────────────┐                                               │
│  │ UploadModal  │                                               │
│  └──────────────┘                                               │
└─────────────────────────────────────────────────────────────────┘
                             │
                             │ HTTP/REST API
                             │ (axios)
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BACKEND API SERVER                          │
│                    (Express.js + Node.js)                        │
│                   http://localhost:5000                          │
│                                                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                    API Routes                           │    │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │    │
│  │  │    /search   │ │   /upload    │ │  /documents  │  │    │
│  │  └──────────────┘ └──────────────┘ └──────────────┘  │    │
│  └────────────────────────────────────────────────────────┘    │
│                             │                                     │
│                             ▼                                     │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              Core Services Layer                        │    │
│  │                                                          │    │
│  │  ┌──────────────────────────────────────────────────┐  │    │
│  │  │         DocumentIndexer Service                  │  │    │
│  │  │  - Manages document index                        │  │    │
│  │  │  - Coordinates parsing & categorization          │  │    │
│  │  │  - Handles search queries                        │  │    │
│  │  │  - Monitors file system                          │  │    │
│  │  └──────────────────────────────────────────────────┘  │    │
│  │                                                          │    │
│  │  ┌──────────────────────────────────────────────────┐  │    │
│  │  │         DocumentParser Service                   │  │    │
│  │  │  - Extracts text from PDFs                       │  │    │
│  │  │  - Extracts text from DOCX                       │  │    │
│  │  │  - Parses TXT, MD, HTML                          │  │    │
│  │  └──────────────────────────────────────────────────┘  │    │
│  │                                                          │    │
│  │  ┌──────────────────────────────────────────────────┐  │    │
│  │  │      DocumentCategorizer Service                 │  │    │
│  │  │  - Analyzes content with NLP                     │  │    │
│  │  │  - Assigns categories (11 types)                 │  │    │
│  │  │  - Identifies teams (6 types)                    │  │    │
│  │  │  - Extracts project names                        │  │    │
│  │  └──────────────────────────────────────────────────┘  │    │
│  └────────────────────────────────────────────────────────┘    │
│                             │                                     │
│                             ▼                                     │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              Search Engine (Lunr.js)                    │    │
│  │  - Full-text indexing                                   │    │
│  │  - Relevance scoring                                    │    │
│  │  - Fast queries                                         │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FILE SYSTEM                                 │
│                                                                   │
│  ┌──────────────────┐         ┌──────────────────┐             │
│  │   /documents     │         │     /index       │             │
│  │   (Your files)   │         │  (Search index)  │             │
│  │                  │         │                  │             │
│  │  ├── file1.pdf   │         │  ├── index.json  │             │
│  │  ├── file2.docx  │         │  └── metadata.json             │
│  │  ├── file3.txt   │         │                  │             │
│  │  └── ...         │         │                  │             │
│  └──────────────────┘         └──────────────────┘             │
│           ▲                                                      │
│           │                                                      │
│  ┌────────┴────────┐                                            │
│  │  File Watcher   │                                            │
│  │   (chokidar)    │                                            │
│  │  - Monitors add  │                                            │
│  │  - Monitors edit │                                            │
│  │  - Monitors del  │                                            │
│  └─────────────────┘                                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow

### 1. Document Indexing Flow

```
File Added/Changed
    │
    ▼
File Watcher Detects Change
    │
    ▼
DocumentParser Extracts Text
    │  ├─ PDF → pdf-parse
    │  ├─ DOCX → mammoth
    │  ├─ TXT → fs.readFile
    │  └─ MD → fs.readFile
    ▼
DocumentCategorizer Analyzes
    │  ├─ Extract keywords (natural)
    │  ├─ Match category patterns
    │  ├─ Identify team
    │  └─ Extract project
    ▼
Document Object Created
    │  {
    │    id, filename, content,
    │    categories, team, project,
    │    preview, keywords, metadata
    │  }
    ▼
Lunr.js Index Updated
    │
    ▼
Metadata Saved to Disk
    │
    ▼
Document Ready for Search ✓
```

### 2. Search Request Flow

```
User Types Query
    │
    ▼
SearchBar Component
    │
    ▼
API Request (axios)
    │  GET /api/search?q=query&filters...
    ▼
Express Route Handler
    │
    ▼
DocumentIndexer.search()
    │
    ▼
Lunr.js Query Execution
    │  - Full-text search
    │  - Relevance scoring
    ▼
Apply Filters
    │  - extension
    │  - category
    │  - project
    │  - team
    │  - dateRange
    ▼
Sort by Score
    │
    ▼
Return Results (JSON)
    │
    ▼
SearchResults Component
    │
    ▼
Display Cards with:
    - Preview
    - Metadata
    - Actions
    - Tags
```

### 3. Upload Flow

```
User Selects Files
    │
    ▼
UploadModal Component
    │  - Validate types
    │  - Check sizes
    ▼
FormData Created
    │
    ▼
POST /api/upload
    │
    ▼
Multer Middleware
    │  - Validate
    │  - Save to /documents
    ▼
File Saved
    │
    ▼
Auto-Indexing Triggered
    │
    ▼
Document Indexed
    │
    ▼
Success Response
    │
    ▼
UI Updates
```

---

## Component Hierarchy

```
App
├── SearchBar
├── Statistics
│   └── StatCard (x4)
├── Filters
│   ├── FilterGroup (x5)
│   └── ClearButton
├── SearchResults
│   └── ResultCard (per result)
│       ├── FileIcon
│       ├── FileDetails
│       ├── Actions (view, download)
│       ├── Preview
│       ├── Metadata
│       └── Tags
└── UploadModal
    ├── UploadArea
    ├── FilesList
    ├── ProgressBar
    └── StatusMessage
```

---

## API Endpoints

```
GET  /api/health
     → Health check

GET  /api/search?q=query&extension=&category=&project=&team=&dateFrom=&dateTo=
     → Search documents

GET  /api/search/filters
     → Get available filters

GET  /api/search/stats
     → Get statistics

POST /api/upload
     → Upload files (multipart/form-data)

GET  /api/documents
     → List all documents

GET  /api/documents/:id
     → Get document by ID

GET  /files/:path
     → Serve static files
```

---

## Database Schema (In-Memory)

```javascript
Map<id, Document> where Document = {
  id: UUID,
  filename: string,
  path: string,
  fullPath: string,
  content: string,
  extension: string,
  size: number,
  created: Date,
  modified: Date,
  categories: string[],
  project: string,
  team: string,
  keywords: string[],
  preview: string
}
```

---

## State Management

### Backend State
- `documents` Map - All indexed documents
- `index` - Lunr.js search index
- File watcher instance
- Metadata cache

### Frontend State (React)
- `searchQuery` - Current search
- `searchResults` - Search results
- `filters` - Active filters
- `availableFilters` - Filter options
- `stats` - Statistics
- `loading` - Loading state
- `showUploadModal` - Modal visibility

---

## Technology Integration

```
┌─────────────────────┐
│   React (Frontend)  │
│  - State management │
│  - Component logic  │
│  - UI rendering     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   Axios (HTTP)      │
│  - API requests     │
│  - Error handling   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Express (Backend)  │
│  - Routing          │
│  - Middleware       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Lunr.js (Search)   │
│  - Indexing         │
│  - Querying         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Natural (NLP)      │
│  - Tokenization     │
│  - Analysis         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  File System        │
│  - Documents        │
│  - Index cache      │
└─────────────────────┘
```

---

## Scalability Considerations

- **Index caching** - Persistent storage for fast startup
- **Incremental updates** - Only re-index changed files
- **Parallel processing** - Multiple files indexed simultaneously
- **Lazy loading** - Content loaded on demand
- **API pagination** - Ready for large result sets
- **Efficient queries** - Optimized Lunr.js configuration

---

**This architecture ensures fast, reliable, and scalable document search!** 🚀
